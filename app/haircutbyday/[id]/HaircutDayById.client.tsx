"use client";

// import { useMoonStore } from "@/store/calendarStore";
import { useParams } from "next/navigation";

export default function HaircutDayByIDClient() {
  // const { id } = useParams<{ id: string }>();
  // const { fetchHaircutDayByID } = useMoonStore();
  // if (!id || typeof id !== "string") {
  //   return <p>Невірний №</p>;
  // }
  // const day = fetchHaircutDayByID(id);
  // return <div>{day?.health}</div>;
}
